# 家庭药箱：设置说明

全程在浏览器里完成，不用装任何软件。

## 第一步：放到网上（GitHub Pages，免费）

1. 打开 https://github.com 注册一个账号。
2. 右上角 **+** → **New repository**。
   - Repository name 填 `yaoxiang`（随意）。
   - 选 **Public**，然后点 **Create repository**。
3. 在新仓库页面点 **uploading an existing file**，把压缩包里的这 7 个文件拖进去：
   `index.html`、`manifest.json`、`sw.js`、`icon-180.png`、`icon-192.png`、`icon-512.png`、`设置说明.md`
   然后点 **Commit changes**。
4. 进入仓库的 **Settings** → 左侧 **Pages**：
   - Source 选 **Deploy from a branch**。
   - Branch 选 **main**，文件夹选 **/ (root)**，点 **Save**。
5. 等 1～2 分钟，页面顶部会出现网址，类似 `https://你的用户名.github.io/yaoxiang/`。
6. 用 iPhone 的 **Safari** 打开这个网址，点分享按钮，选 **添加到主屏幕**。

到这一步就能用了，数据保存在这台手机上。

## 第二步：开启云同步（Firebase，免费）

做完这一步，换手机时只要登录，数据就都回来了。

1. 用你**自己的 Google 账号**打开 https://console.firebase.google.com
   - 点 **Create a project**，名字随意。
   - Google Analytics 可以关掉。
2. 左侧 **Build** → **Authentication** → **Get started** → **Sign-in method**：
   - 选 **Email/Password**，打开第一个开关，点 **Save**。
3. 左侧 **Build** → **Firestore Database** → **Create database**：
   - 位置选离你近的，比如 `us-west2`。
   - 模式选 **production mode**。
4. 在 Firestore 里点 **Rules** 标签，把原来的内容全部换成下面这段，然后点 **Publish**：

   ```
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       match /users/{userId}/{document=**} {
         allow read, write: if request.auth != null && request.auth.uid == userId;
       }
     }
   }
   ```

   这段规则的意思是：只有登录的你本人能读写自己的数据。

5. 点左上角齿轮 → **Project settings** → 往下拉到 **Your apps**：
   - 点 **</>**（Web），随便起个名字，点 **Register app**。
   - 页面会显示一段 `const firebaseConfig = { ... }`，复制大括号里的内容。
6. 回到 GitHub 仓库，点 `index.html` → 右上角铅笔图标（Edit）。
   - 找到这一行：`const FIREBASE_CONFIG = null;`
   - 改成下面这样（用你复制的内容），然后点 **Commit changes**：

   ```js
   const FIREBASE_CONFIG = {
     apiKey: "……",
     authDomain: "……",
     projectId: "……",
     storageBucket: "……",
     messagingSenderId: "……",
     appId: "……"
   };
   ```

   这段配置放在公开网页里是正常的，安全性由第 4 步的规则保证。

7. 等 1～2 分钟，重新打开 App，会出现登录页。
   - 第一次用：点「第一次用，注册」，填你的邮箱和密码。
   - 以后换手机：直接登录即可。

**注意：** 如果开启云同步之前已经在手机上记过药，数据不会自动搬过去。可以按这个顺序操作：

1. 改配置之前，先在 App 里点「设置 → 导出备份」。
2. 开启云同步并登录后，点「设置 → 导入备份」。

## 以后更新 App

我改好新版 `index.html` 后，在 GitHub 仓库里重新上传覆盖就行。

**注意：** 如果你已经填过 Firebase 配置，覆盖后要把配置重新粘贴一次。

## 日历提醒怎么用

- **什么时候会弹出**：添加药品，或者入库一个新的过期日期时，会问你要不要加到日历。
- **怎么加**：点「加到日历」后，iPhone 会弹出日历事件，点 **添加**。它会在过期前 30、60 或 120 天的上午 9 点提醒你。
- **改了提醒天数**：到药品详情页，点批次下面的「加日历提醒」，重新加一次。
- **不看日历也能知道**：App 里药品进入提醒期会变橙色，过期会变红色，列表里的「快过期」也能筛出来。
